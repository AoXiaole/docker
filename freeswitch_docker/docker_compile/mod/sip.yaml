mod_as_sip:
    version: '1.0.14'
    description:
    libs:
        mod_as_sip:
            dependence:
                - mod_as_dbcache
                - mod_as
            load_conf: as.conf.xml,<module name="<module_name>" bus_code="02"/>
    config:
        - sip.xml
